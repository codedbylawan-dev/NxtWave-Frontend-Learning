---
# 📘 **Day 1 – Learning Summary (HTML + CSS Basics + CSS Box Model)**
---

# ⭐ **1. HTML Basics**

### ✔️ Basic Structure of an HTML Document

- `<!DOCTYPE html>` defines HTML5
- `<html>` is the root container
- `<head>` contains metadata (not visible)
- `<body>` contains visible page content

### ✔️ Heading Element (`<h1>` to `<h6>`)

- `<h1>` is the main heading
- `<h2>` to `<h6>` are subheadings
- Used to structure content
- Analogy: Newspaper headlines

### ✔️ Paragraph Element (`<p>`)

- Defines a block of text
- Automatically creates spacing
- Analogy: Storybook sentences

### ✔️ Button Element (`<button>`)

- Creates a clickable button
- Used for actions
- Analogy: A doorbell (click → action)

### ✔️ Div Container (`<div>`)

- Groups elements together
- Helps apply styles
- Analogy: A box holding related items

---

# ⭐ **2. CSS Basics (Part 1)**

### ✔️ CSS Syntax Structure

```css
selector {
  property: value;
}
```

- Selector = what to style
- Property = which style
- Value = how it should look

### ✔️ CSS Text Alignment (`text-align`)

- Values: `left`, `center`, `right`
- Controls horizontal alignment
- Analogy: Telling people where to stand in a group photo

---

# ⭐ **3. CSS Basics (Part 2)**

### ✔️ Text Color (`color`)

- Changes text color
- Accepts color names, hex, rgb

### ✔️ Background Color (`background-color`)

- Sets background color of an element
- Makes card or section stand out
- Analogy: Choosing paper color

---

# ⭐ **4. CSS Font Properties (Part 3)**

### ✔️ Font Family (`font-family`)

- Changes typeface of text
- Must import fonts correctly
- Must use quotation marks
- Analogy: Different handwriting styles

### ✔️ Font Size (`font-size`)

- Controls text size
- Must use `px` (e.g., `20px`)
- No quotes, no spaces

### ✔️ Font Style (`font-style`)

- Values: `normal`, `italic`, `oblique`
- Analogy: Writing styles (normal, slanted, etc.)

---

# ⭐ **5. CSS Box Model – Part 1**

### ✔️ Height (`height`)

- Sets how tall an element is

### ✔️ Width (`width`)

- Sets how wide an element is

### ✔️ Background Image (`background-image`)

- Displays an image behind the element
- Image depends on content height unless specified

### ✔️ Background Size (`background-size`)

- `cover` fills the entire element
- Maintains aspect ratio (W/H)

### ✔️ Viewport Units (`vh` & `vw`)

- `vh` = 1% of screen height
- `vw` = 1% of screen width
- `100vh` → full screen height
- `100vw` → full screen width

---

# ⭐ **6. CSS Box Model – Part 2**

### ✔️ Border Width (`border-width`)

- Sets thickness of border
- `0px` removes the border
- Needs `border-style` to be visible

### ✔️ Border Radius (`border-radius`)

- Rounds corners
- Can round specific corners
- Analogy: Cutting corners of an ID card

### ✔️ Border Color (`border-color`)

- Sets border color
- Also requires `border-style`

---

# 🎉 **Day 1 Covered a LOT!**

You learned:

### 🧩 HTML basics

### 🎨 CSS basics

### 🔤 CSS text & font styling

### 🖼️ CSS backgrounds

### 📦 CSS Box Model (dimension + borders)

### 🌐 Viewport units
