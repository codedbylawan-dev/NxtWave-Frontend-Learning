# ✅ **Length of the String (Solved)**

Write a program that reads a word and prints the number of characters in that word.

---

## **Sample Input**

```
Software
```

## **Sample Output**

```
8
```

---

# 🧭 **Outline**

**Question:** Length of the String
**Approach:**
Step 1: Read the input word
Step 2: Find the length of the word
Step 3: Print the length

---

# 📝 **Step-by-Step Explanation**

## **Step 1: Read the word**

Use the `input()` function to read the string from the user:

```python
message = input()
```

---

## **Step 2: Find the length**

Use Python’s built-in `len()` function to count the number of characters:

```python
length = len(message)
```

---

## **Step 3: Print the result**

Now print the length using `print()`:

```python
print(length)
```

---

# ✅ **Final Solution**

```python
message = input()
length = len(message)
print(length)
```

---
