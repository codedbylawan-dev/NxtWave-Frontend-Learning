# ✅ **Question: Simple Square – 4**

Write a program that prints a simple square using the hash symbol `#`.

The square must have:

- **3 rows**
- **3 columns**
- Each row contains **3 hash symbols**

### **Expected Output**

```
###
###
###
```

---

# ✅ **Approach**

### **Step 1: Understand the problem**

You must print a **3×3 square** made of `#`.
Each line has three `#` symbols.

---

### **Step 2: Write the code**

Use the `print()` function.
Use `" #" * 3` to repeat the hash symbol.

---

# ✅ **Final Code**

```python
print("#" * 3)
print("#" * 3)
print("#" * 3)
```

---

# 🟩 **DRY RUN (Tabular Format)**

| Step No.  | Operation                | Output/Value      |
| --------- | ------------------------ | ----------------- |
| 1         | Execute `print("#" * 3)` | Prints `###`      |
| 2         | Execute `print("#" * 3)` | Prints `###`      |
| 3         | Execute `print("#" * 3)` | Prints `###`      |
| **Final** | Combined output          | ###<br>###<br>### |

---
